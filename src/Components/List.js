import React from "react";
import ListElement from "./ListElement";

const List = () => {
    return (
        <main>
            <ul>
                <ListElement />
            </ul>
        </main>
    );
};

export default List;